import React from "react";

function FunctionComponent(){
// we are not using here render cause it only used for class components.
return(
    <div>
        <p>This is Functional Component</p>
    </div>
)
}
export default FunctionComponent;